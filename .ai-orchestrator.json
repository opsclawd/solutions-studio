{
  "validation": {
    "commands": [
      "pnpm format:fix",
      "pnpm -r --if-present build",
      "pnpm lint",
      "pnpm boundaries",
      "pnpm typecheck",
      "pnpm format",
      "pnpm -r --if-present test"
    ],
    "tiers": [
      ["pnpm format:fix"],
      ["pnpm -r --if-present build"],
      ["pnpm lint", "pnpm boundaries", "pnpm typecheck", "pnpm format", "pnpm -r --if-present test"]
    ],
    "timeout": 600
  }
}
